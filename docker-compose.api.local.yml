# Yerel override — host 3005 → konteyner 3000
# (RetailEX PostgREST 3001–3020 ve Dokploy :3000 ile çakışmasın)
# Kullanım: docker compose -f docker-compose.api.yml -f docker-compose.api.local.yml up -d --build

services:
  finatura_api:
    ports:
      - "3005:3000"
