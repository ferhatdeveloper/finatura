# Mock yalnızca açıkça true iken. Varsayılan: canlı gateway / document-agent
VITE_USE_MOCK=false

# API Gateway (auth + tenant proxy) — varsayılan 3000
VITE_API_GATEWAY_URL=http://localhost:3000

# Document Agent — varsayılan 3100
VITE_DOCUMENT_AGENT_URL=http://localhost:3100
